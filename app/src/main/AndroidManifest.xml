<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newsindproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewsINDProject">
        <activity android:name=".BBCheadlineActivity"></activity>
        <activity android:name=".HealthActivity" />
        <activity android:name=".ScienceActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SportsActivity" />
        <activity android:name=".TechnologyActivity" />
        <activity android:name=".BusinessActivity" />
        <activity android:name=".HeadlineActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>